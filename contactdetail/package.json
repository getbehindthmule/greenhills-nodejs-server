{
  "name": "contactdetail",
  "version": "1.0.0",
  "description": "get contact details via nodejs for greenhills server",
  "main": "app.js",
  "scripts": {
    "pretest": "npm run lint",
    "test": "mocha",
    "lint": "jshint *.js **/*.js",
    "precompile": "npm run clean",
    "clean": "rimraf lib/*",
    "pack": "npm-pack-zip"
  },
  "keywords": [],
  "author": "greenhills",
  "license": "ISC",
  "devDependencies": {
    "aws-sdk-mock": "^5.1.0",
    "chai": "^4.2.0",
    "jshint": "^2.11.0",
    "mocha": "^7.1.1",
    "npm-pack-zip": "^1.2.7",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "aws-sdk": "^2.814.0"
  }
}
